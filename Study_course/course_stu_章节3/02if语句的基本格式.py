"""
if 要判断的条件:
    条件成立时,要做的事
"""
# 定义变量
age = 30
print(f"我已经{age}岁了")
if age >= 18:# 当这条判断语句为真时,代码会被执行,否则不会执行
    print("如果age >= 18 为真则执行这条语句")

print("时间过得真快")

if age <= 18:
    print(f"如果age <= 18 为真则执行这条语句")
    # 可以看到这条语句并没有被执行

